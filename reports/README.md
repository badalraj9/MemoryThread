# Reports

Current report:

- `PITCH_RESULTS_2026-04-17.md`

Current benchmark artifact:

- `throughput_benchmark_2026-04-17_final_batch.json`

Older intermediate benchmark artifacts were removed to prevent confusion. The current safe performance claim is:

> Direct SDK batched writes reach 2.7K-4.8K EPS locally, with 11.85x-17.25x speedup over sync direct SDK in the same benchmark.

Do not use the old unsupported 19.21x claim.
